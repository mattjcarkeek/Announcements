import { UniqueColorPipe } from './unique-color.pipe';

describe('UniqueColorPipe', () => {
  it('create an instance', () => {
    const pipe = new UniqueColorPipe();
    expect(pipe).toBeTruthy();
  });
});
